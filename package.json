{
  "name": "@tak-nz/media-infra",
  "version": "8.3.1",
  "description": "Media infrastructure for TAK-NZ project using AWS CDK",
  "private": true,
  "bin": {
    "cdk": "bin/cdk.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "unset CDK_DEFAULT_ACCOUNT && unset CDK_DEFAULT_REGION && jest",
    "test:watch": "unset CDK_DEFAULT_ACCOUNT && unset CDK_DEFAULT_REGION && jest --watch",
    "test:coverage": "unset CDK_DEFAULT_ACCOUNT && unset CDK_DEFAULT_REGION && jest --coverage",
    "cdk": "cdk",
    "clean": "rm -f bin/*.js bin/*.d.ts lib/*.js lib/*.d.ts lib/constructs/*.js lib/constructs/*.d.ts test/*.js test/*.d.ts; rm -rf cdk.out/*",
    "dev": "npm run build && npm run test",
    "deploy:dev": "npm run build && cdk deploy --context environment=dev-test --no-warnings",
    "deploy:prod": "npm run build && cdk deploy --context environment=prod --no-warnings",
    "deploy:local:dev": "npm run build && cdk deploy --context environment=dev-test --context usePreBuiltImages=false --no-warnings",
    "deploy:local:prod": "npm run build && cdk deploy --context environment=prod --context usePreBuiltImages=false --no-warnings",
    "synth:dev": "npm run build && cdk synth --context environment=dev-test",
    "synth:prod": "npm run build && cdk synth --context environment=prod",
    "cdk:diff:dev": "npm run build && cdk diff --context environment=dev-test",
    "cdk:diff:prod": "npm run build && cdk diff --context environment=prod",
    "cdk:bootstrap": "cdk bootstrap",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "diff": "cdk diff",
    "synth": "cdk synth"
  },
  "engines": {
    "node": ">= 22"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TAK-NZ/media-infra.git"
  },
  "author": "Christian Elsen <chris@elsen.nz>",
  "license": "AGPL-3.0-only",
  "bugs": {
    "url": "https://github.com/TAK-NZ/media-infra/issues"
  },
  "homepage": "https://github.com/TAK-NZ/media-infra#readme",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "^2.1020.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.167.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}